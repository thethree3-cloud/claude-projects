# Topic 1 (continued): Variables — 10 more simple examples
# Same style as TODO 1: one variable, one value. Work through them one at a time.

# TODO 1: Assign your favorite color to a variable called `favorite_color` (str)
favorite_color = "green"

# TODO 2: Assign the current year to a variable called `current_year` (int)
current_year = 2026


# TODO 3: Assign a price with cents to a variable called `price` (float)
price = 5.00

# TODO 4: Assign whether you own a pet to a variable called `has_pet` (bool)
has_pet = True

# TODO 5: Assign your city to a variable called `city` (str)
city = "Salt Lake"

# TODO 6: Assign how many hours you slept last night to a variable called `hours_slept` (float)
hours_slept = 5

# TODO 7: Assign today's temperature to a variable called `temperature` (float)
temperature = 98

# TODO 8: Assign whether it's raining right now to a variable called `is_raining` (bool)
is_raining = False

# TODO 9: Assign your zip code to a variable called `zip_code` (str, not int — zip codes can start with 0)
zip_code = "84119"

# TODO 10: Assign the number of days in a week to a variable called `days_in_week` (int)
days_in_week = 7


# Leave this section as-is — it checks your work once the TODOs are filled in.
print(favorite_color, type(favorite_color))
print(current_year, type(current_year))
print(price, type(price))
print(has_pet, type(has_pet))
print(city, type(city))
print(hours_slept, type(hours_slept))
print(temperature, type(temperature))
print(is_raining, type(is_raining))
print(zip_code, type(zip_code))
print(days_in_week, type(days_in_week))
